============================================================================================================================
# Listar todos os metabólitos do modelo
for met in model.metabolites:
    print(f"ID: {met.id}, Nome: {met.name}")

============================================================================================================================
#Pesquisa de metabólito por id

id_metabolite = "actn__R_e"

if id_metabolite in [met.id for met in model.metabolites]:
    print(f"O metabólito '{id_metabolite}' existe no modelo.")
else:
    print(f"O metabólito '{id_metabolite}' não foi encontrado no modelo.")

============================================================================================================================
#Pesquisa de informaçãos do metabolito

id_metabolite = "btoh[c]"


metabolito = model.metabolites.get_by_id(id_metabolite)

print(f"ID do Metabólito: {metabolito.id}")
print(f"Nome: {metabolito.name}")
print(f"Fórmula: {metabolito.formula}")
print(f"Compartimento: {metabolito.compartment}")
print(f"Carregamento (Charge): {metabolito.charge}")
print(f"Reações associadas: {[r.id for r in metabolito.reactions]}")
    
metabolito.summary()   

============================================================================================================================

#exibindo os limites de um metabólito específico

print(model.reactions.EX_glyc_e.bounds) 

============================================================================================================================

#Pesquisa de metabólito por nome

nome_metabolite = "pyruvate"

if nome_metabolite in [met.name for met in model.metabolites]:
    print(f"O metabólito '{nome_metabolite}' existe no modelo.")
else:
    print(f"O metabólito '{nome_metabolite}' não foi encontrado no modelo.")

